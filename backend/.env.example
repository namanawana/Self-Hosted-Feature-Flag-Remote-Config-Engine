# Copy this file to .env and fill in your values
# cp .env.example .env

# API key used to protect developer routes (create, delete, archive flags/configs)
# Choose any strong secret string — this is checked via the x-api-key header
API_KEY=your_secret_key_here
